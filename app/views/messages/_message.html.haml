.message-wide{"data-id": "#{message.id}"}
  .upper-message
    .upper-message__user-name
      = message.user.name
    .upper-message__date
      = message.created_at.to_formatted_s(:without_second)
  .lower-message__content
    = message.content
    = image_tag message.image if message.image.present?