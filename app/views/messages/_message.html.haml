.message
  .upper-message
    %p.upper-message__user-name
      = message.user.name
    %p.upper-message__date
      = message.created_at
  - if message.content.present?
    %span.lower-message__content
      = message.content
    = image_tag message.image.url, class: 'lower-message__image' if message.image.present?